# Gateway origin (browser + SSR both use it). Inlined into the client bundle
# at build time — rebuild after changing it.
NEXT_PUBLIC_GATEWAY_URL=https://communist-bevvy-ritchermap-12836c60.koyeb.app
